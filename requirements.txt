# Core ML & Backtesting
backtrader
protobuf==5.28.3
tensorflow
torch
scikit-learn>=1.8.0
pandas
numpy
joblib
matplotlib
seaborn
tqdm
# pandas-ta
TA-Lib

# AWS & API
sagemaker>=2.248.1,<3
boto3
uvicorn
fastapi
pydantic>=2.0.0
pydantic-settings

# Crypto Data
ccxt
python-binance
cryptography
requests
feedparser
aiohttp
praw
google-genai>=1.0.0

# Utilities
python-dotenv
setuptools
wheel
typing-extensions>=4.7.0
loguru
streamlit
plotly
openai

# Calibration baseline. Loaded by src/calibration_agent/ml_service.py when
# XGBOOST_MODEL_PATH points at a joblib-saved model.
xgboost>=3.0,<4.0

# Live trading state. Redis is the source of truth for the live trader's
# open positions / pending orders / daily PnL (see src/state/position_store.py).
# fakeredis is test-only but pinned here because the unit suite depends on it.
redis>=5.0,<6.0
fakeredis>=2.20,<3.0

# Monitoring (Phase 7). Both surfaces are env-var toggled via SENTRY_DSN /
# PROMETHEUS_PUSH_URL; the live supervisor degrades to a no-op when unset.
sentry-sdk>=2.0,<3.0
prometheus-client>=0.20,<1.0

# Tooling
ruff>=0.6,<1.0
